python==3.8
conda install pytorch==2.0.0 torchvision==0.15.0 torchaudio==2.0.0 pytorch-cuda=11.8 -c pytorch -c nvidia

pip install -r requirements/build.txt
pip install -v -e .


ERROR: Failed building wheel for lap ==> pip install lapx

ERROR: Failed building wheel for mmcv-full ==> pip install -U openmim;  mim install 'mmcv>=1.6.1,<1.7.0';mim install 'mmdet>=2.19.1,<3.0.0'
;mim install 'mmcls>=0.16.0,<1.0.0'

mim install -r requirements/runtime.txt ==> There is a 'lap' line in this file and we have loaded the alternative version of this library, 
namely 'lapx'. So we have to delete the line that 'lap' and run it



configs/det/faster-rcnn_r50_fpn_4e_mot17-half.py added ===> device = 'cuda'


tools/train.py     
    # log env info
    #env_info_dict = collect_env() 
    #env_info = '\n'.join([(f'{k}: {v}') for k, v in env_info_dict.items()])
    #dash_line = '-' * 60 + '\n'
    #logger.info('Environment info:\n' + dash_line + env_info + '\n' +dash_line)
    # meta['env_info'] = env_info
The collect_env() function was commented out because it did not work properly.