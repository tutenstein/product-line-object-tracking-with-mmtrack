# Using Python 3.8
python==3.8

# Installing PyTorch, TorchVision, and Torchaudio with CUDA 11.8
conda install pytorch==2.0.0 torchvision==0.15.0 torchaudio==2.0.0 pytorch-cuda=11.8 -c pytorch -c nvidia

# Installing required dependencies
pip install -r requirements/build.txt
pip install -v -e .

# Encountered an error with lap library, using lapx as an alternative
ERROR: Failed building wheel for lap ==> pip install lapx

# Encountered an error with mmcv-full library, using mim to install instead
ERROR: Failed building wheel for mmcv-full ==>
pip install -U openmim
mim install 'mmcv>=1.6.1,<1.7.0'
mim install 'mmdet>=2.19.1,<3.0.0'
mim install 'mmcls>=0.16.0,<1.0.0'

# In the runtime.txt file, there is a 'lap' entry which we need to remove since we've loaded the alternative 'lapx' library
mim install -r requirements/runtime.txt
# ==> The 'lap' line should be deleted after installing 'lapx', then run the command again.

# Added CUDA device in the configuration file
configs/det/faster-rcnn_r50_fpn_4e_mot17-half.py ==> device = 'cuda'

# The collect_env() function was commented out due to improper functioning
tools/train.py     
    # log env info
    #env_info_dict = collect_env() 
    #env_info = '\n'.join([(f'{k}: {v}') for k, v in env_info_dict.items()])
    #dash_line = '-' * 60 + '\n'
    #logger.info('Environment info:\n' + dash_line + env_info + '\n' + dash line)
    # meta['env_info'] = env_info
# The collect_env() function was commented out because it didn't work properly.
